#!/bin/bash
for i in $( ls tests/double); do
  echo =====================================================
  echo item: $i
  ./a.out < tests/double/$i
  echo =====================================================  
  echo =====================================================
 
done
