#!/bin/bash
for i in $( ls tests/int); do
  echo =====================================================
  echo item: $i
  ./a.out < tests/int/$i
  echo =====================================================  
  echo =====================================================
 
done
